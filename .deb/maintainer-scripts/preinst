#!/bin/sh
set -e

ROOT_DIR="/opt/fwcloud/agent"

# If we are upgrading the FWCloud-Agent package, then preserve the .env config file.
if [ "$1" = "upgrade" ]; then
  mv -f "${ROOT_DIR}/.env" "${ROOT_DIR}/.env.upgrade"
fi

exit 0

